name: seagraph
channels:
  - conda-forge

dependencies:
  - python=3.11
  - jupyterlab
  - numpy
  - pandas
  - matplotlib
  - seaborn
  - pip
  - pip:
    - -e ..   # installs dissector from the repo root (run conda env create from eval_notebooks/)
